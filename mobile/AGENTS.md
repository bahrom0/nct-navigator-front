# NCT Navigator Mobile

- Read `../mobile_version_md/AGENTS.md`, the active session file, and the three local mobile skills before changing this app.
- This is an independent Expo SDK 57 project. Read the exact versioned Expo 57 docs before using an Expo API.
- Use native React Native views and Expo development builds. Do not add WebView-based product screens.
- Keep API contracts, auth, maintenance behavior, and streaming semantics aligned with the existing backend.
- Do not add dependencies to the repository root for mobile-only features.
- Run `npm run lint`, `npm run typecheck`, and `npm test` before handoff.
